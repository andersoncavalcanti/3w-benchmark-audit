# Python 3.10 or later.
#
# Versions below are lower bounds known to work. Pin them exactly
# (== instead of >=) if you need byte-identical reproduction.

polars>=0.20          # Parquet I/O and dataframe operations
pyarrow>=14.0         # Parquet backend for polars
numpy>=1.24           # array operations, run-length and difference analysis
scikit-learn>=1.3     # classifiers, cross-validation, metrics
matplotlib>=3.7       # figures
